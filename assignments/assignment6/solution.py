# Assignment 6 — Loops
# =====================
# Use loops to process task lists.


def count_completed(tasks: list) -> int:
    """
    Count the number of tasks with status 'completed'.
    Each task is a dict with at least a 'status' key.
    Example: count_completed([{'status': 'completed'}, {'status': 'pending'}]) → 1
    """
    # TODO: implement using a for loop
    return 0


def sum_durations(tasks: list) -> float:
    """
    Sum the 'duration' values of all tasks in the list.
    Each task is a dict with a 'duration' key (float, minutes).
    Example: sum_durations([{'duration': 30}, {'duration': 45}]) → 75.0
    """
    # TODO: implement using a for loop
    return 0.0


def find_longest_task(tasks: list) -> dict:
    """
    Return the task dict with the highest 'duration'.
    Assume the list is non-empty.
    """
    # TODO: implement
    return {}


def generate_daily_targets(start: int, end: int, step: int) -> list:
    """
    Return a list of integers from start to end (inclusive) with the given step.
    Example: generate_daily_targets(1, 10, 2) → [1, 3, 5, 7, 9]
    """
    # TODO: implement using range()
    return []
