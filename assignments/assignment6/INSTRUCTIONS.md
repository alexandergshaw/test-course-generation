# Assignment 6 — Loops: Task Iteration

**Week 6 | Topic: for loops, while loops, range()**

You will write functions that iterate over task lists to compute statistics for the dashboard's chart section.

---

## Learning Goals

- Use `for` loops to iterate over lists.
- Accumulate values with a running total.
- Use `while` loops for condition-driven repetition.
- Use `range()` to generate numeric sequences.

---

## Instructions

### Step 1 — Open `solution.py`

Open `assignments/assignment6/solution.py` and read each stub function.

---

### Step 2 — Implement Each Function

Work through `count_completed`, `sum_durations`, `find_longest_task`, and `generate_daily_targets` one at a time. The docstrings describe the expected behaviour and include examples.

> 💡 Use a `for` loop and an accumulator variable for the first three functions.

---

### Step 3 — Save, Test, and Submit

1. Save the file.
2. Run all tests in the **Testing** panel (all should be green ✅).
3. Stage → commit (`assignment6: implement loops`) → push → PR.

---

## ✅ Completion Checklist

- [ ] All four functions implemented and correct.
- [ ] All tests pass.
- [ ] PR submitted on GitHub.
- [ ] Dashboard card for Assignment 6 shows ✅.
