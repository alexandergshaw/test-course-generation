# Review 9 — Loops, Functions & Data Structures
# ================================================


def flatten(nested: list) -> list:
    """
    Flatten a one-level nested list.
    Example: flatten([[1, 2], [3, 4]]) → [1, 2, 3, 4]
    """
    # TODO: implement
    return []


def word_frequency(text: str) -> dict:
    """
    Return a dict mapping each word (lowercase) to how many times it appears.
    Ignore punctuation — split only on whitespace.
    Example: word_frequency("hi hi there") → {'hi': 2, 'there': 1}
    """
    # TODO: implement
    return {}


def running_total(numbers: list) -> list:
    """
    Return a list where each element is the cumulative sum up to that index.
    Example: running_total([1, 2, 3]) → [1, 3, 6]
    """
    # TODO: implement
    return []


def merge_dicts(d1: dict, d2: dict) -> dict:
    """
    Return a new dict that is the merge of d1 and d2.
    If a key exists in both, the value from d2 takes precedence.
    """
    # TODO: implement
    return {}


def deduplicate(items: list) -> list:
    """
    Return a new list with duplicate values removed, preserving order.
    Example: deduplicate([3, 1, 2, 1, 3]) → [3, 1, 2]
    """
    # TODO: implement
    return []
