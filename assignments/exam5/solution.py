# Exam 5 — Test 1 Practice
# ==========================
# This is a timed practice exam covering Weeks 1–4.
# Complete all functions. You may use any techniques from the course so far.
# No external libraries are needed.


def reverse_string(text: str) -> str:
    """Return text reversed. Example: 'hello' → 'olleh'."""
    # TODO: implement
    return ""


def is_palindrome(text: str) -> bool:
    """Return True if text reads the same forwards and backwards (case-insensitive, ignore spaces)."""
    # TODO: implement
    return False


def fizzbuzz(n: int) -> str:
    """
    Classic FizzBuzz for a single number:
    Divisible by both 3 and 5 → 'FizzBuzz'
    Divisible by 3 only       → 'Fizz'
    Divisible by 5 only       → 'Buzz'
    Otherwise                 → str(n)
    """
    # TODO: implement
    return ""


def calculate_average(numbers: list) -> float:
    """Return the arithmetic mean of a non-empty list of numbers."""
    # TODO: implement
    return 0.0


def letter_grade(score: int) -> str:
    """Map score to letter grade: A(90+), B(80+), C(70+), D(60+), F(<60)."""
    # TODO: implement
    return ""
