# Assignment 8 — Data Structures
# =================================


def filter_by_status(tasks: list, status: str) -> list:
    """
    Return a new list containing only tasks where task['status'] == status.
    Example: filter_by_status(tasks, 'completed') → [...]
    """
    # TODO: implement
    return []


def group_by_category(tasks: list) -> dict:
    """
    Return a dict mapping each category to a list of tasks in that category.
    Each task has a 'category' key.
    Example: {'work': [{...}, {...}], 'personal': [{...}]}
    """
    # TODO: implement
    return {}


def unique_categories(tasks: list) -> set:
    """
    Return a set of all unique category strings found in the task list.
    """
    # TODO: implement
    return set()


def top_n_tasks(tasks: list, n: int) -> list:
    """
    Return the top n tasks sorted by 'duration' in descending order.
    If n > len(tasks), return all tasks sorted.
    """
    # TODO: implement
    return []
