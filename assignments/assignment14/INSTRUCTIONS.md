# Assignment 14 — Advanced Features

**Week 14 | Topic: Decorators, Generators, Comprehensions, Context Managers**

Add advanced Python features to the task pipeline for cleaner, more Pythonic code.

---

## Learning Goals

- Write and apply a function **decorator**.
- Create a **generator** function using `yield`.
- Use **list comprehensions** and **dictionary comprehensions**.
- Use the `with` statement as a context manager.

---

## Instructions

### Step 1 — Open `solution.py`

Open `assignments/assignment14/solution.py`. You have four functions/components to implement.

---

### Step 2 — Implement Each Item

| Item | Concept |
|---|---|
| `log_call` decorator | Wrap a function to print its name on call |
| `task_generator` | Generator yielding tasks one at a time |
| `completed_names` | List comprehension over task dicts |
| `duration_map` | Dict comprehension mapping name → duration |

---

### Step 3 — Save, Test, and Submit

1. Save the file.
2. All tests green ✅.
3. Stage → commit (`assignment14: implement advanced features`) → push → PR.

---

## ✅ Completion Checklist

- [ ] `log_call` decorator works correctly.
- [ ] `task_generator` yields tasks one by one.
- [ ] `completed_names` uses a list comprehension.
- [ ] `duration_map` uses a dict comprehension.
- [ ] All tests pass.
- [ ] PR submitted on GitHub.
- [ ] Dashboard card for Assignment 14 shows ✅.
